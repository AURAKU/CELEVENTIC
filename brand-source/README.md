# Official Celeventic brand source

Save the **official logo image** (full logo with icon + wordmark) as:

```
brand-source/celeventic-official.png
```

Then run:

```bash
npm run brand:assets
```

This generates all app icons and header logos under `public/brand/` and `src/app/`.
